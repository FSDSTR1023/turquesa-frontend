import { Link } from 'react-router-dom';
import '../styles/CampoAEditar.css';

const CampoAEditar = (campo) => {
    console.log(campo);
    return (
        <div className="campoAEditar">
            
        </div>
    );
}

export default CampoAEditar;